<<<<<<< HEAD

# safelist
branches:
  only:
  - master

language: cpp
compiler: gcc
os:
  - linux
  - osx


before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then mkdir $HOME/usr && export PATH="$HOME/usr/bin:$PATH" && wget https://cmake.org/files/v3.7/cmake-3.7.2-Linux-x86_64.sh && chmod +x cmake-3.7.2-Linux-x86_64.sh && ./cmake-3.7.2-Linux-x86_64.sh --prefix=$HOME/usr --exclude-subdir --skip-license; fi
install: mkdir build && cd build && rm -fR * && cmake ..

before_script:

script: make
=======
sudo: required
language: cpp

matrix:
  include:
# Makefile builder
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=make LANGUAGE=cc
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=make LANGUAGE=python2
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=make LANGUAGE=python3
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=make LANGUAGE=java
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=make LANGUAGE=dotnet

    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=cc
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=python2
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=python3
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=java
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=dotnet
# CMake Build Generator
    - os: linux
      dist: trusty
      compiler: gcc
      env: BUILDER=cmake
    - os: linux
      dist: trusty
      compiler: clang
      env: BUILDER=cmake

    - os: osx
      osx_image: xcode9.4
      compiler: gcc
      env: BUILDER=cmake
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=cmake
# Jobs too long
  allow_failures:
    - os: osx
      osx_image: xcode9.4
      compiler: clang
      env: BUILDER=make LANGUAGE=dotnet

install:
  - ./.travis/install.sh

before_script:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then export JAVA_HOME=`/usr/libexec/java_home -v 1.8`; fi

script:
  - ./.travis/script.sh
>>>>>>> 1292f58e7bf595362b92451962c4c10e27007613
